export declare enum TestRunStatus {
    RUNNING = "RUNNING",
    PASSED = "PASSED",
    FAILED = "FAILED",
    APP_HALTED = "APP_HALTED",
    USER_ABORT = "USER_ABORT",
    APP_FAULT = "APP_FAULT",
    INTERNAL_ERR = "INTERNAL_ERR"
}
interface RunOptions {
    delay: number;
    debug: boolean;
    port: number;
    path: string;
}
export declare const Test: (appCmd: string, runOptions: RunOptions, callback: (err: Error | null, result?: boolean) => void) => Promise<void>;
export {};
